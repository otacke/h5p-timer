var H5P = H5P || {};

/**
 * H5P notifications (used by Timer)
 *
 * stores notifications that evoke callbacks
 *
 * @param {H5P.jQuery} $
 */
H5P.Timer.Notifications = (function ($) {
  /**
   * Create a timer.
   * @constructor
   */
  function Notifications() {
    var self = this;
  }
  
  return Notifications;
})(H5P.jQuery);